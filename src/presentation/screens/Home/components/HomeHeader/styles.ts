import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    flexDirection: "row",
    gap: 8,
  },
  leftContent: {
    flex: 1,
    flexDirection: "row",
    gap: 8,
  }
});